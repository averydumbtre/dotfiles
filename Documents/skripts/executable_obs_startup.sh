wait 30
pkill -SIGHUP obs
wait 1
pkill -SIGHUP obs
wait 1
pkill -SIGHUP obs
wait 1
pkill -SIGHUP obs
wait 1
pkill -SIGHUP obs
wait 1
obs --multi --startreplaybuffer --minimize-to-tray --disable-shutdown-check
quit