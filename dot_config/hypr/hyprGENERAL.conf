#### my hyprland config file :3

## PROGRAM DEFS AND EXECS -~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-
$terminal = alacritty
$screenshot = hyprshot -m region --clipboard-only -z

$menu = tofi-drun --fuzzy-match false --font /usr/share/fonts/adobe-source-code-pro/SourceCodePro-Regular.otf --hint-font false --ascii-input true --drun-launch=true --font-size=16 --background-color=5355bdAA --outline-width=2 --outline-color=d389ed --border-width=0 --border-color=95bcff --text-color=d6efff --prompt-text=Hmm? --prompt-padding=30 --prompt-color=5355bd --input-color=d6efff --selection-color=ffe39b --num-results=0 --selection-background=f864b6 --selection-background-corner-radius=6 --selection-background-padding=2,4 --result-spacing=10 --width=800 --height=400 --prompt-background=ff4f78 --prompt-background-padding=2,4 --prompt-background-corner-radius=7 --clip-to-padding=false --padding-top=20 --padding-left=5 --corner-radius=50 --placeholder-text="What do you want?" --placeholder-background=f864b6 --placeholder-background-padding=2,400,2,4 --placeholder-background-corner-radius=7 --input-background=f864b6 --input-background-padding=2,400,2,4 --input-background-corner-radius=7 --default-result-background=d389ed  --default-result-background-padding=4,2 --default-result-background-corner-radius=7

exec-once = systemctl --user enable opentabletdriver.service --now
exec-once = solaar --window=hide
permission = /usr/(bin|local/bin)/hyprpm, plugin, allow
exec-once = hyprpm enable dynamic-cursors
exec-once = hyprpm reload -n
exec-once = wal -R
exec-once = mpd
exec-once = sleep 5 && waybar
exec-once = easyeffects -w --gapplication-service
exec-once = syncthing

exec=hyprctl setcursor elementary 30
env = XCURSOR_THEME,elementary
env = XCURSOR_SIZE,30

exec-once=swaync
exec-once = systemctl --user start hyprpolkitagent
exec-once = systemctl --user import-environment QT_QPA_PLATFORMTHEME

#####
##env = NVD_BACKEND,direct
##env = ELECTRON_OZONE_PLATFORM_HINT,auto
##env = XDG_CURRENT_DESKTOP,Hyprland
##env = XDG_SESSION_TYPE,wayland
##env = XDG_SESSION_DESKTOP,Hyprland


## open qt6ct for qt theming, nwg-look for gtk
env = QT_QPA_PLATFORMTHEME,qt6ct


## MOUSE SENS + KEYBOARD BINDS-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-

input {
    kb_layout = us
    repeat_rate = 43
    repeat_delay = 333
    accel_profile = flat
    force_no_accel = false
    follow_mouse = 1
}

### 1600 dpi
#device {
#    name = logitech-g305-1
#    sensitivity = -0.6
#    accel_profile = flat
#    natural_scroll = false
#}

## hide waybar
##bindt = , Alt_L, exec, pkill -SIGUSR1 waybar
##bindrt = ALT, Alt_L, exec, pkill -SIGUSR1 waybar

## exits hyprland ???
##bind = SUPER, M, exit,

## unused obs replay bind
##bind = SUPER&Shift_L, S, pass, ^(com\.obsproject\.Studio)$
##bind = SUPER&Shift_L, S, pass, obs
##bind = SUPER&Shift_L, S, pass, ^obs$

##hyprland easymotion plugin
##bind = SUPER, z, easymotion, action:hyprctl dispatch focuswindow address:{}

## open apps
bind = SUPER, 36, exec, $menu
bind = SUPER, Q, exec, alacritty
bind = SUPER, S, exec, librewolf
bind = SUPER SHIFT, S, exec, librewolf --private-window
bind = SUPER, E, exec, easyeffects
bind = SUPER, V, exec, vesktop
bind = SUPER, T, exec, thunar /home/avery/Downloads
bind = SUPER, M, exec, alacritty --hold & sleep 0.5 && wtype rmpc -k Return
bind = SUPER, P, exec, $screenshot,
bind = SUPER, I, exec, alacritty --hold & sleep 0.5 && wtype iamb -k Return
bind = SUPER, N, exec, nheko
bind = SUPER, R, exec, alacritty --hold & sleep 0.5 && wtype ranger -k Return
bind = SUPER, B, exec, alacritty --hold & sleep 0.5 && wtype btop -k Return
bind = SUPER, D, exec, obsidian
bind = SUPER SHIFT, D, exec, NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia __VK_LAYER_NV_optimus=NVIDIA_only davinci-resolve-studio

## move/edit apps
bind = SUPER, F, exec, hyprctl --batch "dispatch togglefloating ; dispatch centerwindow 1"
bind = SUPER SHIFT, f, fullscreen
bind = SUPER, C, killactive,
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7
bind = SUPER SHIFT, 8, movetoworkspace, 8
bind = SUPER SHIFT, 9, movetoworkspace, 9
bind = SUPER SHIFT, 0, movetoworkspace, 10
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow
bindm = SUPER SHIFT,  mouse:272, resizewindow

#vim keybinds
bind = SUPER, h, movefocus, l
bind = SUPER, l, movefocus, r
bind = SUPER, k, movefocus, u
bind = SUPER, j, movefocus, d

bind = SUPER SHIFT, l, movewindow, r
bind = SUPER SHIFT, h, movewindow, l
bind = SUPER SHIFT, k, movewindow, u
bind = SUPER SHIFT, j, movewindow, d


binde = SUPER CONTROL, l, resizeactive, 30 0
binde = SUPER CONTROL, h, resizeactive, -30 0
binde = SUPER CONTROL, k, resizeactive, 0 -30
binde = SUPER CONTROL, j, resizeactive, 0 30


bind = SUPER SHIFT, up, movetoworkspace, -1
bind = SUPER SHIFT, down, movetoworkspace, +1

bind = SUPER, up, workspace, -1
bind = SUPER, down, workspace, +1


## move focus
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9
bind = SUPER, 0, workspace, 10

## volume/brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
binde = SUPER, code:59, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
binde = SUPER, code:60, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+

## german
##bind = , y, exec, wtype z
##bind = , z, exec, wtype y

bind = ALT, u, exec, wtype ü
bind = ALT_SHIFT, u, exec, wtype Ü

bind = ALT, o, exec, wtype ö
bind = ALT_SHIFT, o, exec, wtype Ö

bind = ALT, a, exec, wtype ä
bind = ALT_SHIFT, a, exec, wtype Ä
env = ELECTRON_OZONE_PLATFORM_HINT,auto

bind = ALT, s, exec, wtype ß
bind = ALT_SHIFT, s, exec, wtype ẞ

## misc
bind = SUPER, O, exec, bash ~/Documents/skripts/monitoroff.sh
bind = SUPER, I, exec, bash ~/Documents/skripts/monitoron.sh
bind = SUPER SHIFT, O, exec, bash ~/Documents/skripts/monitormainoff.sh
bind = SUPER SHIFT, I, exec, bash ~/Documents/skripts/monitormainon.sh
bind = SUPER, G, exec, bash ~/Documents/skripts/gamemode.sh
bind = SUPER SHIFT, W, exec, bash ~/Documents/skripts/WarframeRes.sh

bind = SUPER, mouse_down, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor * 1.25}')"
bind = SUPER, mouse_up, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor / 1.25}')"
bind = SUPER SHIFT, mouse_up, exec, hyprctl keyword cursor:zoom_factor 1
binds:scroll_event_delay = 0

## sends a signal via hdmi to a real monitor, not a laptop! and only works for max/min
##bind = SUPER, B, exec, bash ~/Documents/skripts/brightness.sh
##bind = SUPER, N, exec, bash ~/Documents/skripts/bright.sh

## LOOK -~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-

general {
    border_size = 3
    no_border_on_floating = false
    gaps_in = 3
    gaps_out = 3
    resize_on_border = true
    allow_tearing = false
    layout = master
    col.active_border = rgb(ff4f78)
    col.inactive_border = rgb(5355bd)
}

master{
   allow_small_split = false
    new_status = inherit
    new_on_top = false
    new_on_active = after
    drop_at_cursor = false
}

xwayland {
  force_zero_scaling = true
}

layerrule = noanim, selection

decoration {
    rounding = 3
    inactive_opacity = 0.8
    dim_inactive = true
    dim_strength = 0.2
    blur {
        size = 5
        passes = 2
        vibrancy = 0.1693
        new_optimizations = true
        layerrule = blur,waybar
        layerrule = ignorezero,waybar
        layerrule = blur,launcher
        layerrule = ignorezero,launcher
    }
}


animations {
    enabled = true
    bezier = myBezier, 0.10, 0.9, 0.1, 1.05
    animation = windowsIn, 1, 5, myBezier, slide
    animation = windows, 1, 5, myBezier, slide
    animation = windowsOut, 1, 5, myBezier, slide
    animation = windowsMove, 1, 2, myBezier, slide
    animation = border, 1, 10, myBezier
    animation = fade, 1, 7, myBezier
    animation = workspaces, 1, 5, myBezier, slidevert
    animation = workspacesIn, 1, 5, myBezier, slidevert
    animation = workspacesOut, 1, 5, myBezier, slidevert
    animation = layers, 1, 4, myBezier, slidevert
}

animation = windows, 1, 3, default, popin 80%
animation = workspaces, 1, 8, default, slidefade 20%


dwindle {
    preserve_split = false
    smart_split = true
    pseudotile = true
    use_active_for_splits = false
}

misc {
    disable_hyprland_logo = true
##    render_ahead_of_time = false
##    render_ahead_safezone = 2
    animate_manual_resizes = true
    animate_mouse_windowdragging = true
}

render {
    direct_scanout = false
}

cursor {
    sync_gsettings_theme = true
    inactive_timeout = 10
}

xwayland {
    create_abstract_socket = true
}

#plugin:easymotion {
#    textsize=30
#    textcolor=rgb(d6efff)
#    bgcolor=rgb(5355bd)
#    blur=1
#    blurA=0.8
#    xray=0
#    textfont=Sans
#    textpadding=30
#    bordersize=5
#    bordercolor=rgb(ff4f78)
#    rounding=3
#    fullscreen_action=none
#    montionkeys=abcdefghijklmnopqrstuvwxyz1234567890
#}



plugin:dynamic-cursors {

    # enables the plugin
    enabled = true

    # sets the cursor behaviour, supports these values:
    # tilt    - tilt the cursor based on x-velocity
    # rotate  - rotate the cursor based on movement direction
    # stretch - stretch the cursor shape based on direction and velocity
    # none    - do not change the cursors behaviour
    mode = tilt

    # minimum angle difference in degrees after which the shape is changed
    # smaller values are smoother, but more expensive for hw cursors
    threshold = 2
    # for mode = rotate
    rotate {

        # length in px of the simulated stick used to rotate the cursor
        # most realistic if this is your actual cursor size
        length = 20

        # clockwise offset applied to the angle in degrees
        # this will apply to ALL shapes
        offset = 0.0
    }

    # for mode = tilt
    tilt {

        # controls how powerful the tilt is, the lower, the more power
        # this value controls at which speed (px/s) the full tilt is reached
        limit = 6900

        # relationship between speed and tilt, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used (most realistic to actual air drag)
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        function = negative_quadratic
    }

    # for mode = stretch
    stretch {

        # controls how much the cursor is stretched
        # this value controls at which speed (px/s) the full stretch is reached
        limit = 3000

        # relationship between speed and stretch amount, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        function = quadratic
    }

    # configure shake to find
    # magnifies the cursor if its is being shaken
    shake {

        # enables shake to find
        enabled = true

        # use nearest-neighbour (pixelated) scaling when shaking
        # may look weird when effects are enabled
        nearest = false

        # controls how soon a shake is detected
        # lower values mean sooner
        threshold = 6.0

        # magnification level immediately after shake start
        base = 4.0
        # magnification increase per second when continuing to shake
        speed = 4.0
        # how much the speed is influenced by the current shake intensitiy
        influence = 0.0

        # maximal magnification the cursor can reach
        # values below 1 disable the limit (e.g. 0)
        limit = 0.0

        # time in millseconds the cursor will stay magnified after a shake has ended
        timeout = 500

        # show cursor behaviour `tilt`, `rotate`, etc. while shaking
        effects = true

        # enable ipc events for shake
        # see the `ipc` section below
        ipc = false
    }

    # use hyprcursor to get a higher resolution texture when the cursor is magnified
    # see the `hyprcursor` section below
    hyprcursor {

        # use nearest-neighbour (pixelated) scaling when magnifing beyond texture size
        # this will also have effect without hyprcursor support being enabled
        # 0 / false - never use pixelated scaling
        # 1 / true  - use pixelated when no highres image
        # 2         - always use pixleated scaling
        nearest = true

        # enable dedicated hyprcursor support
        enabled = true

        # resolution in pixels to load the magnified shapes at
        # be warned that loading a very high-resolution image will take a long time and might impact memory consumption
        # -1 means we use [normal cursor size] * [shake:base option]
        resolution = -1

        # shape to use when clientside cursors are being magnified
        # see the shape-name property of shape rules for possible names
        # specifying clientside will use the actual shape, but will be pixelated
        fallback = clientside
    }
}


## OTHER -~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-~-

##windowrulev2 = opacity 0.0 override, class:^(xwaylandvideobridge)$
##windowrulev2 = noanim, class:^(xwaylandvideobridge)$
##windowrulev2 = noinitialfocus, class:^(xwaylandvideobridge)$
##windowrulev2 = maxsize 1 1, class:^(xwaylandvideobridge)$
##windowrulev2 = noblur, class:^(xwaylandvideobridge)$



windowrulev2 = nodim, class:mpv
windowrulev2 = opacity 1.0 override, class:mpv
windowrulev2 = nodim, class:scrcpy
windowrulev2 = opacity 1.0 override, class:scrcpy
windowrulev2 = nodim, class:com.moonlight_stream.Moonlight
windowrulev2 = opacity 1.0 override, class:com.moonlight_stream.Moonlight
windowrulev2 = nodim, title:(.*)(- YouTube).*$
windowrulev2 = opacity 1.0 override, title:(.*)(- YouTube).*$
windowrulev2 = nodim, title:^.*(Twitch).*$
windowrulev2 = opacity 1.0 override, title:^.*(Twitch).*$
windowrulev2 = nodim, title:^.*(darktable).*$
windowrulev2 = opacity 1.0 override, title:^.*(darktable).*$
windowrulev2 = nodim, title:^.*(Picture-in-Picture).*$
windowrulev2 = opacity 1.0 override, title:^.*(Picture-in-Picture).*$

layerrule = blur, waybar
layerrule = blurpopups, waybar
layerrule = ignorealpha 0.01, waybar # this is so entirely transparent things aren't blurred.


# Replicate old "smart gaps"/"no gaps when only" option
##workspace = w[tv1], gapsout:0, gapsin:0
##workspace = f[1], gapsout:0, gapsin:0
##windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
##windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
##windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
##windowrulev2 = rounding 0, floating:0, onworkspace:f[1]
##windowrulev2 = float, class:nm-tray
